# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                       Application.index

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

# Catch all
*       /{controller}/{action}                  {controller}.{action}

# Import CRUD routes
*     /admin           module:crud


#API V1

##User Related
GET /api/v1/User/details/all   controllers.ApplicationApiV1.getUserList()
GET /api/v1/User/details/{userId}   controllers.ApplicationApiV1.getUserDetails(userId: Long)

GET /api/v1/User/subscriptions/{userId}  controllers.ApplicationApiV1.getUserRouteSubsciption(userId: Long)




##Checkin Related
GET /api/v1/User/checkin/{userId}  controllers.ApplicationApiV1.getUserLastCheckinLocation(userId: Long)